// function removeForm() {
//   document.getElementById("edit_task_<%= @task.id %>").remove();
// }

// removeForm();

// function refreshCounterTask() {
//   const counterTask = document.getElementById("counter_tasks");
//   counterTask.innerHTML = '<%= pluralize current_user.count_tasks, "task"%>';
// };
// refreshCounterTask();

var dashboard = document.querySelector('.wrapper-dashboard');
var potShow = document.querySelector('#container-pot-show');

if (dashboard) {
  dashboard.innerHTML = '<%= j render "./pages/dashboard-container", user: @user, pots: @pots %>';
};
if (potShow){
  potShow.innerHTML = '<%= j render "./pots/pot_content", pot: @pot %>';
}

bindSweetAlertButtonDemo();
updateTaskStatus();
