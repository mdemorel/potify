<div class="task-statistics">
  <% if pot.tasks.where(completed: true).count > 0 %>
    <h4>You took care of <%= pot.name %> <%= pluralize pot.tasks.where(completed: true).count, "time" %> since you met 😘</h4>
  <% end %>

<!--   <div class="row">
    <%# pot.tasks.each do |task| %>
      <div class="col-xs-12 col-md-6">
        <%# unless task.nil? %>
          <div class="task-follow-up">
            <h5><%#= task.name.capitalize %></h5>

            <p>Last <%#= task.name %> : <%#= pot.send("last_#{task.name}").strftime("%B %d, %Y") %></p>
            <p>Next <%#= task.name %> : <%#= pot.next_task_date(task.name).strftime("%B %d, %Y") %></p>
            <!-- <p><%#= pluralize pot.tasks.where(name: task.name).count, "time" %> since you met</p> -->
          <!-- </div> -->
        <%# end %>
     <!--  </div> -->
    <%# end %>
  <!-- </div> -->

  <div class="row">
    <%# Tasks::TASKS.each do |task_name| %>
      <% pot.tasks.where(completed: false).each do |task| %>
        <div class="col-xs-12 col-md-6">
          <% unless task.nil? %>
            <div class="task-follow-up">
              <h5><%= task.name.capitalize %></h5>
              <p>Last <%= task.name %> : <%= pot.send("last_#{task.name}").strftime("%B %d, %Y") %></p>
              <p>Next <%= task.name %> : <%= pot.next_task_date(task.name).strftime("%B %d, %Y") %></p>
              <!-- <p><%#= pluralize pot.tasks.where(name: task.name).count, "time" %> since you met</p> -->
            </div>
          <% end %>
        </div>
      <% end %>
    <%# end %>
  </div>
</div>
