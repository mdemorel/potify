<div class="follow-up-card">
  <div class="row">
    <% pot.tasks.last(4).each do |task| %>
    <div class="col-xs-12 col-md-6">
      <div >
        <% unless task.nil? %>
          <div >
            <h5><%= task.name.capitalize %></h5>
            <div>
              <div>
                <% if pot.tasks.where(completed: true, name: task.name).size > 0 %>
                  <p class="action-date">Last performed on
                  <span class="no-padding-left"><%= pot.send("last_#{task.name}").strftime("%B %d, %Y") %>
                  </span>
                <% end %>
              </p>
              </div>
              <div >
                <p class="action-date">You need to <%= task.action_name %> <%= pot.name.capitalize %> before
                  <span class="no-padding-left"><%= pot.next_task_date(task.name).strftime("%B %d, %Y") %></span>
                </p>
              </div>
            </div>
            <!-- <p><%#= pluralize pot.tasks.where(name: task.name).count, "time" %> since you met</p> -->
          </div>
        <% end %>
      </div>
    </div>
    <% end %>
  </div>
</div>
